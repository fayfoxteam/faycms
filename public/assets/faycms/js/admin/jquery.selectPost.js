/**
 * 弹出文章列表，选择一篇或多篇文章
 */
;(function($){
    $.extend({
        'selectPost': function(params){
            var defaults = {
                'type': 'multiple',//multiple: 多选；single: 单选
                
            };
        }
    });
})(jQuery);